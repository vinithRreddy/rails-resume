<%= form_with(model: additional_information,url:user_additional_informations_path,method: :post, local: true) do |form| %>
  <% if additional_information.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(additional_information.errors.count, "error") %> prohibited this additional_information from being saved:</h2>

      <ul>
        <% additional_information.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :achievements %>
    <%= form.text_area :achievements %>
  </div>

  <div class="field">
    <%= form.label :hobbies %>
    <%= form.text_area :hobbies %>
  </div>

  <div class="field">
    <%= form.label :strengths %>
    <%= form.text_area :strengths %>
  </div>

  <div class="field">
    <%= form.label :weaknesses %>
    <%= form.text_area :weaknesses %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
