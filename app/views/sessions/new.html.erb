<div class="container jumbotron">
<%= form_with(scope: :session,url:login_path,local:true) do |f| %>
    <% if User.new.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(User.new.errors.count, "error") %> prohibited this comment from being saved:</h2>
      <ul>
        <% User.new.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
    <%= f.label :name %>
    <%= f.text_field :name %>
    <%= f.label :password %>
    <%= f.password_field :password %>
    <%= f.submit "Log In" %>
<% end %>
</div>
