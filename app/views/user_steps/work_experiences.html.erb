

<%= form_with(model: @user, url: wizard_path, local: true) do |form| %>


    <div data-controller="nested-form">
      <template data-target='nested-form.template'>
        <%= form.fields_for :work_experiences,  @user.work_experiences, child_index: 'TEMPLATE_RECORD' do |work_experience| %>
          <%= render 'work_experiences_fields', form: work_experience %>
        <% end %>
      </template>
  
      <%= form.fields_for :work_experiences do |work_experience| %>
        <%= render 'work_experiences_fields', form: work_experience %>
      <% end %>

      
    </div>
    <div class="flex">
            <div data-target="nested-form.add_item"  class="flex justify-end ml-56 items-center bg-blue-600 text-white rounded hover:bg-blue-500 px-4 py-2 focus:outline-none" href="resume.html"><span class="material-icons pr-1" style="font-size: 18px;"></span>

      <%= link_to "Add", "#", data: { action: "nested-form#add_association" },:class => 'bg-blue-600 text-white' %>
          </div>
           <div data-target="nested-form"  class=" action flex justify-end  items-center ml-5 bg-blue-600 rounded  px-4 py-2 focus:outline-none" href="resume.html"><span class="material-icons pr-1" style="font-size: 18px;"></span>
            <%= form.submit "Save and Continue",:class => 'bg-blue-600 text-white'%>
            </div>
    
  <% end %>